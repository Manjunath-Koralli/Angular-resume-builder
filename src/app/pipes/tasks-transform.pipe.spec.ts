import { TasksTransformPipe } from './tasks-transform.pipe';

describe('TasksTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new TasksTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
